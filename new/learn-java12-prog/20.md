# 评估

# 第 1 章 - 使用 Java 12 开始

1.  c）Java 开发套件
2.  b）Java 类库
3.  d）Java 标准版
4.  b）综合开发环境
5.  a）项目建设，b）项目配置，c）项目文件
6.  a）布尔，b）数字
7.  a）长，c）短，d）字节
8.  d）价值表示
9.  a））\\，b）2_0，c）2__0f，d）\ f
10.  a）％，c）＆，d） - >
11.  a）0
12.  b）假，假
13.  d）4
14.  c）编译错误
15.  b）2
16.  A，C，D
17.  d）20 -1
18.  c） *x* 值在 11 范围内
19.  c）结果= 32
20.  a）可以声明一个变量，b）可以分配变量
21.  b）选择陈述，d）增量陈述

# 第 2 章 - java 面向对象编程（OOP）

1.  广告
2.  B，C，D
3.  A，B，C
4.  A，C，D
5.  D.
6.  C，D
7.  A，B
8.  B，D
9.  D.
10.  B.
11.  A，C.
12.  B，C，D
13.  A，B
14.  B，C
15.  B，C，D
16.  B，C
17.  C
18.  A，B，C
19.  B，C，D
20.  A，C.
21.  A，C，D

# 第 3 章 - Java 基础知识

1.  广告
2.  C，D
3.  A，B，D
4.  A，C，D
5.  A，C.
6.  A，B，D
7.  A B C D
8.  C，D
9.  D.
10.  C
11.  B.
12.  C

# 第 4 章 - 例外处理

1.  A，B，C
2.  B.
3.  C
4.  A B C D
5.  一个
6.  A，C.
7.  D.

# 第 5 章 - 字符串，输入/输出和文件

1.  B.
2.  C
3.  B.
4.  一个
5.  D.
6.  A，C，D
7.  C
8.  D.
9.  A，B，C
10.  C，D（注意使用`mkdir()`方法而不是`mkdirs()`）

# 第 6 章 - 数据结构，泛型和流行公用事业

1.  D.
2.  B，D
3.  A B C D
4.  A B C D
5.  A，B，D
6.  A，B，C
7.  C
8.  A B C D
9.  B，D
10.  B.
11.  B，C
12.  一个
13.  C
14.  D.
15.  B.
16.  C
17.  一个
18.  B.
19.  C

# 第 7 章 - Java 标准和外部库

1.  A，B，C
2.  A，B，D
3.  B，C
4.  B，D
5.  A，C.
6.  A B C D
7.  B，C，D
8.  B，C
9.  B.
10.  C，D
11.  A，C.
12.  B，D
13.  广告
14.  B，C，D
15.  A，B，D
16.  B，D

# 第 8 章 - 多线程和并发处理

1.  A，C，D
2.  B，C，D
3.  一个
4.  A，C，D
5.  B，C，D
6.  A B C D
7.  C，D
8.  A，B，C
9.  B，C
10.  B，C，D
11.  A，B，C
12.  B，C
13.  B，C

# 第 9 章 - JVM 结构和垃圾收集

1.  B，D
2.  C
3.  D.
4.  B，C
5.  广告
6.  C
7.  A B C D
8.  A，C，D
9.  B，D
10.  A B C D
11.  一个
12.  A，B，C
13.  A，C.
14.  A，C，D
15.  B，D

# 第 10 章 - 管理数据库中的数据

1.  C
2.  广告
3.  B，C，D
4.  A B C D
5.  A，B，C
6.  广告
7.  A，B，C
8.  A，C.
9.  A，C，D
10.  A，B
11.  广告
12.  A，B，D
13.  A，B，C

# 第 11 章 - 网络编程

1.  正确的答案可能包括 FTP，SMTP，HTTP，HTTPS，WebSocket，SSH，Telnet，LDAP，DNS 或其他一些协议
2.  正确的答案可能包括 UDP，TCP，SCTP，DCCP 或其他一些协议
3.  `java.net.http`
4.  UDP.
5.  是的
6.  `java.net`
7.  传输控制协议
8.  它们是同义词
9.  通过 IP 地址和端口的源和 IP 地址和目标端口
10.  可以在没有客户端运行的情况下使用`ServerSocket`。 它只是在指定端口上“侦听”
11.  UDP.
12.  TCP.
13.  正确的答案可能包括 http，https，telnet，f​​tp 或 smtp
14.  A，C，D
15.  它们是同义词
16.  它们是同义词
17.  `/something/something?par=42`
18.  正确的答案可能包括二进制格式，标题压缩，多路复用或推送能力
19.  `java.net.http.HttpClient`
20.  `java.net.http.WebSocket`
21.  没有不同
22.  `java.util.concurrent.CompletableFuture`

# Chapter 12 – Java GUI Programming

1.  阶段
2.  节点
3.  应用
4.  `void start(Stage pm)`
5.  `static void launch(String... args)`
6.  `--module-path`和`--add-modules`
7.  `void stop()`
8.  `WebView`
9.  `Media`，`MediaPlayer`，`MediaView`
10.  `--add-exports`
11.  以下列表中的任何五个：`Blend`，`Bloom`，`BoxBlur`，`ColorAdjust`，`DisplacementMap`，`DropShadow`，`DropShadow`，`Glow`，`InnerShadow`，`InnerShadow`，`InnerShadow`，`MotionBlur`，`MotionBlur`，`PerspectiveTransform` ，`Reflection`，`ShadowTone`和`SepiaTone`

# 第 13 章 - 功能规划

1.  C
2.  广告
3.  一
4.  `void`
5.  一
6.  `boolean`
7.  没有任何
8.  `T`
9.  一
10.  `R`
11.  封闭的背景
12.  `Location::methodName`

# 第 14 章 - Java 标准流

1.  A，B
2.  `of()`没有参数产生空流
3.  `java.util.Set`
4.  135
5.  42
6.  2121
7.  不，但它扩展了功能界面`Consumer`，可以随着这样传递
8.  没有任何
9.  3
10.  1.5
11.  “42，x，a”
12.  编译错误因为`peek()`无法返回任何内容
13.  2
14.  替代`Optional`对象
15.  “一个”
16.  一
17.  任何`filter()`，`map()`和`flatMap()`
18.  任何`distinct()`，`limit()`，`sorted()`，`reduce()`和`collect()`

# 第 15 章 - 反应性编程

1.  A，B，C
2.  是的
3.  非阻塞输入/输出
4.  不
5.  反应延伸
6.  `java.util.concurrent`
7.  广告
8.  阻止操作员名称以“阻塞”开头
9.  热情可观察到的速度可发出值。 在前一个到达终端操作员后，感冒可观察到的可观察者发出下一个值
10.  可观察到的停止发射值，管道停止运行
11.  A，C，D
12.  例如，来自以下任意的任何两个：`buffer()`，`flatMap()`，`groupBy()`，`map()`，`scan()`，`window()`

13.  例如，来自以下任意的任何两个：`debounce()`，`distinct()`，`elementAt(long n)`，`filter()`，`filter()`，`ignoreElements()`，`ignoreElements()`，`ignoreElements()`，`sample()`，`sample()`，`sample()`，`take()`
14.  删除过度值，采取最新的，使用缓冲区
15.  `subscribeOn()`，`observeOn()`，`fromFuture()`

# 第 16 章 - 微服务

1.  A，C.
2.  是的
3.  传统应用程序的方式，加上他们经常拥有自己的方式来沟通（例如，使用事件总线）
4.  来自列表中的任何两个：Akka，DropWizard，Jodd，LightBend Lagom，Ninja，Spotify Apollo，Vert.x.
5.  实现界面的类`io.vertx.core.Verticle`
6.  `Send`只发送一个在地址注册的一个接收器; `publish`向所有接收器发送消息，以与同一地址注册
7.  它使用循环算法
8.  是的
9.  [https://vertx.io/](https://vertx.io/)

# 第 17 章 - Java Microbenchmark Marness

1.  B，C，D
2.  将 JMH 依赖性添加到项目（或类路径，如果手动运行）并将注释`@Benchmark`添加到您想要测试性能的方法
3.  作为使用具有明确命名的主类的 Java 命令的主要方法，作为使用 Java 命令使用可执行文件`.jar`文件的主要方法，并使用 IDE 作为主要方法或使用插件并运行单个方法
4.  以下任一项：`Mode.AverageTime`，`Mode.Throughput`，`Mode.SampleTime`，`Mode.SingleShotTime`

5.  以下两种：`TimeUnit.NANOSECONDS`，`TimeUnit.MICROSECONDS`，`TimeUnit.MILLISECONDS`，`TimeUnit.SECONDS`，`TimeUnit.MINUTES`，`TimeUnit.MINUTES`，`TimeUnit.HOURS`，`TimeUnit.DAYS`
6.  使用带注释的类的对象`@State`
7.  在`state`属性前使用注释`@Param`
8.  使用注释`@CompilerConrol`
9.  使用消耗所产生的结果的`Blackhole`的参数
10.  使用注释`@Fork`

# 第 18 章 - 写作高质量代码的最佳实践

1.  A，B，C
2.  通常，建议但不需要。 虽然对于某些情况，但是，当例如将放置并搜索基于哈希的数据结构时，需要某些情况
3.  `obj1`是“少”而不是`obj2`
4.  不
5.  `StringBuilder`
6.  在不更改客户端代码的情况下允许实现更改
7.  更多控制代码演进和代码灵活性，以适应更改
8.  更可靠的代码，更快地编写，测试更少，更容易别人了解
9.  其他程序员将在以后维护代码和您的代码
10.  不，但对你来说非常有帮助